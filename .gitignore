# Generated by configure.bat — machine-specific, do not commit
.env
livekit.yaml
ingress.yaml

# AWS deployment artifacts — machine-specific / secrets, never commit
*.pem
.sg_id.txt
docker-compose.override.yml
